# 逆向任务交接摘要（2026-08-11）

> 本文件由 reasonix 会话 `20260805-233312` 崩溃后整理，供新会话继续工作。
> 原会话因上下文达到模型真实上限（1048576 tokens）无法继续，已放弃；本摘要覆盖其全部关键成果与下一步。

---

## 一、任务背景

用户最初目标：**逆向"蜗牛实用工具"**（`D:\传奇工具\蜗牛实用工具6.3.2\蜗牛实用工具.exe`，SHA256 E69D4FA3…）。
性质：传奇（Mir2 私服/开服）服务端一体化工具，作者"蜗牛"，官网 www.9wnpc.com。

后续会话中额外完成了**虾米工具箱**（Python/PyInstaller 工具）的逆向。

工作目录：`C:\Users\Administrator\AppData\Roaming\reasonix\global-workspace\snail-reversing\`
（解包产物在 `extracted/`；大量分析脚本 `peek-*/extract-*/dump-*/parse-jsc-*` 保留在根目录，可复用。）

---

## 二、蜗牛实用工具（Electron 22.3.27）逆向成果

技术栈画像：Electron 22.3.27（V8 10.8.168.25）· Vue3+Vite+Element Plus · Node16 · better-sqlite3 · node-adodb · ffi-napi。
用户数据目录 `C:\snail\`。app.asar 108MB / 4892 文件，含 1500+ 哈希假条目反解包混淆（已绕过）。

### 保护机制与破解状态
| 层 | 保护 | 状态 |
|---|---|---|
| 主进程 `out/main/index.jsc`（176KB） | bytenode V8 字节码 | ✅ 已用 Electron 22.3.27 mock 环境加载执行 |
| preload `out/preload/index.jsc`（1.5MB） | 同上（含全部数据层逻辑） | ✅ 已加载执行 |
| 渲染进程 `out/renderer/assets/*.js` | terser + `\uXXXX` 转义 | ✅ 明文可读 |
| 反调试/反注入/多开检测 | inspector/injection/pipe 检测 | ✅ mock 绕过 |
| snail.dat 授权 DLL（161KB 加密） | 完整性校验 | ✅ 已解密（`snail-decrypted.bin` 158KB）；**内部逻辑未分析**（需 Ghidra/IDA，本机无反汇编器） |

关键加载技术（V8 cached data 头）：`magic|version_hash|source_hash|flag_hash`，version_hash `866ceba8` 只在 Electron 22.3.27 的 V8 上匹配；加载需 `--no-lazy --no-flush-bytecode` 并覆盖 flag_hash。

### 还原度（用户 8/11 问"95% 还原是啥意思"，agent 的诚实回答）
1. **渲染进程 = 源码级 ✅**：`out/renderer/assets/*.js` 解码后完整可读（爆率调整、物品库、注入、同步、微端等模块的组件/表单/调用逻辑，仅变量名被压成 e/t/n）。
2. **主进程+preload = 行为级 ⚠️**：拿到全部函数名、IPC 通道、SQL、数据库表结构、密钥、URL、初始化真实行为；但**函数体算法**（如爆率调整具体怎么解析 MonGen.txt/写回）是从字符串+SQL+调用关系**推断**的，非逐行还原。
3. **native DLL（授权模块）未分析 ⚠️**。

### 关键产物（snail-reversing/ 根目录）
| 文件 | 内容 |
|---|---|
| `REVERSE-REPORT.md`（61KB） | **主报告**：应用画像/保护机制/完整功能清单，最全 |
| `main-disasm.txt`、`preload-disasm.txt`（各 4.6MB） | V8 `--print-bytecode` 官方反汇编（main 347 函数/115 操作码/最大 1951 指令） |
| `main-funcs-annotated.txt`、`preload-funcs-annotated.txt`（各 ~1MB） | **已含常量池值的注释版反汇编**（指令+常量池条目对应） |
| `main-funcs-pseudocode.txt`、`preload-funcs-pseudocode.txt`（各 ~650KB） | 伪代码还原 |
| `bc-constant-pools.txt`（171KB） | 常量池提取 |
| `jsc-main-strings.txt`、`jsc-preload-strings.txt`、`jsc-preload-utf16.txt`、`jsc-cn.txt` | 字符串/中文提取 |
| `snail-decrypted.bin`（158KB） | 授权 DLL 解密体 |
| `爆率功能业务逻辑.md` 等 *.md | 单模块业务逻辑说明 |
| `v8-serializer.cc`/`v8-code-serializer.cc`/`v8-deserializer.cc`/`v8-bytecodes.h` | V8 序列化源码（研究 .jsc 格式用，已抓取） |

---

## 三、虾米工具箱（Python/PyInstaller）逆向成果（附带给用户的额外项）

方法：PyInstaller CArchive 解包（20001 流）→ 补 py3.8 头 → pycdc/pycdas/uncompyle6 反编译。
- **业务模块源码级 ✅**：`npc_visual_v2_page.py`、`visual_spawn_page.py`、`toolbox_native_core.py`、`toolbox_core_rpc.py`、`embedded_droprate_template.py` 等 8+ 模块完整源码（存 `xiami-decompiled/` 等目录）。
- **主程序 `工具箱_qt.py`（2.5MB，297 顶层函数）= 结构级 ⚠️**：`main-structure.txt`（518KB 函数树+签名+字符串）+ `main_0014_disasm.txt`（38MB pycdas 指令级反汇编）；pycdc 对超大模块静默失败、uncompyle6 语法失败，源码未逐行还原但功能逻辑可还原。
- 报告：`xiami-代码级还原报告.md`、`xiami136-逆向报告.md`、`xiami-完整功能清单.md`。

---

## 四、当前卡点与下一步建议

1. **native DLL（蜗牛授权模块）**：`snail-decrypted.bin` 已解密，但内部逻辑未分析 → 需要 Ghidra/IDA（本机没有，可下载安装，或先做 strings/导入表静态摸底）。
2. **主进程语义级还原**：若用户要"业务逻辑逐行跑出来"，下一步是把 `main-funcs-annotated.txt`（指令+常量池）配合 `main-funcs-pseudocode.txt` 按函数逐段还原成可读 JS（工作量按功能模块分批）。
3. **虾米主程序源码**：可选尝试新反编译器（如 decompyle3 补丁、pycdc 升级版）挑战 2.5MB 大模块。
4. 原会话遗留问题：若再打开 `20260805-233312` 会话会因超限失败——**别在旧会话里继续**，用本摘要即可。

---

## 五、给用户的直接答复（来自原会话 8/11）

"95% 还原" = **理解层面**的还原，不是把源码逐行跑出来：
- 渲染进程：源码级可读 ✅
- 主进程/preload：行为级（函数/IPC/SQL/表结构/密钥/URL + 初始化行为），算法由推断得出 ⚠️
- native DLL：未分析 ⚠️