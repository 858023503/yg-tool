# 元歌工具箱 功能缺陷修复清单(可执行)

> 生成日期:2026-08-10
> 修复对象:`D:\元歌工具箱\GUI\resources\app\lib.js`(主)、`main.js`、`preload.js`、`renderer.js`、`m2-reload.ps1`
> 行号以当前文件为准;改完一行后后续行号会偏移,按函数名定位。
> 约定:每条 =【位置】【现状】【原版依据】【改法】【验证】。改前先备份 `lib.js`;改完用 `node --check lib.js` 验语法。
> 严重度:P0=错误数据/扣错货币/核心功能失效;P1=常见路径功能错误;P2=边界/兼容/一致性。

---

## 一、P0 必改(数据/金钱/核心功能)

### P0-1 货币兑换/回收:灵符/荣誉/积分映射自相矛盾
- 【位置】`lib.js:1103`(CURRENCY_CMDS.gameglory)、`lib.js:1106`(CURRENCY_CMDS.gamegird)
- 【现状】`gameglory: { name:'灵符', check:'CHECKGameGlory' … }`、`gamegird: { name:'积分', check:'CHECKGameGird' … }`。选"灵符"生成 `CHECKGameGlory`(实际操作荣誉),选"积分"生成 `CHECKGameGird`(实际操作灵符)。与 `lib.js:872` 自身 `matchCurrencyCmd` 的映射(GAMEGIRD:'灵符')相反。
- 【原版依据】`REVERSE-REPORT.md:148`:`GAMEGIRD=灵符、GAMEGLORY=荣誉`。
- 【改法】仅改两个 name 字段:
  ```js
  gameglory: { …, name: '荣誉', … }   // 行 1103: '灵符' → '荣誉'
  gamegird:  { …, name: '灵符', … }   // 行 1106: '积分' → '灵符'
  ```
  并全局检查 UI 下拉 key(`renderer.js` 兑换/回收面板的 `<option value="gameglory">灵符</option>` 文案同步改为"荣誉"、`gamegird` 文案改"灵符")。
- 【验证】面板选"灵符"生成脚本,应出现 `CHECKGameGird`/`GameGird`;选"积分"不再出现。

### P0-2 普通爆率分组:组头固定 `#CHILD 1/1 RANDOM`,爆率放大近百倍 + 嵌套 #CHILD
- 【位置】`lib.js:230`(groupRates 的 flush)、`lib.js:238-241`(收集循环)
- 【现状】组头写死 `{ kind:'child', num:1, rate:1 }` → `#CHILD 1/1 RANDOM`;收集时对已存在 #CHILD 块内子项(kind=rate)不区分,遇 `)` 才 flush,已有随机组的文件再分组会产出嵌套 `#CHILD 1/10 ( #CHILD 1/1 (…) )`。
- 【原版依据】虾米 `工具箱_qt_recovered.py:10644-10657` `_drop_group_text`:按**相同分母**归组,组头保留原分母 `"#CHILD 1/%d RANDOM" % rate`;`10634-10639` 用 `in_child` 状态显式跳过块内行。
- 【改法】重写 groupRates:
  1. 组头 rate 取该批第一条普通行的分母:`newItems.push({ kind:'child', num:1, rate: batch[0].rate, flag:'RANDOM', … })`;
  2. 遍历时维护括号深度 `depth`(`(` 进、`)` 出),`depth>0` 的行直接透传不参与 pending;
  3. 子项行保持原 num/rate 不变(不要改成 1/1)。
- 【验证】构造含 5 条 `1/100` 的文件分组 → 输出 `#CHILD 1/100 RANDOM ( 1/100 a … )`;对含 `#CHILD 1/10 RANDOM (…)` 的文件再分组 → 不得出现嵌套 `#CHILD`。

### P0-3 开区网站:爆率 den 换算方向颠倒 + 分子被丢
- 【位置】`lib.js:3626-3631`(exportSiteData)
- 【现状】`den = it.rate >= 100 ? 1 : Math.round(100/it.rate)`。`it.rate` 是 `1/N` 的**分母 N**(parseLine:35),不是百分比 → `1/1` 变 den=100、`1/22` 变 den=5;`it.num`(分子)被忽略,`5/10` 显示成 1/10。
- 【原版依据】虾米 `工具箱_qt_recovered.py:10619/10809/10821`:正则 `^\s*1/(\d+)` 取 denominator,写回 `1/%d`;模板 `site-template/droprate.html:644,656` 渲染 `'1/'+d.den`。
- 【改法】
  ```js
  // rate 即分母;num>1 时折算 den = round(rate/num),下限 1
  const den = it.num > 1 ? Math.max(1, Math.round(it.rate / it.num)) : (it.rate >= 1 ? it.rate : 0);
  ```
  child 行同理:`den = it.rate`(不再除以 100)。
- 【验证】`1/1` → den=1、`1/22` → den=22、`5/10` → den=2;生成 site 后 droprate.html 显示与爆率文件一致。

### P0-4 个人/动态刷怪 QManage 模式失效:跳转段不存在 + SETONTIMER 缺间隔参数
- 【位置】`lib.js:2766`(SETONTIMER 注入)、`lib.js:2773`(OnTimer 段内容)、调用方 `lib.js:2675-2731`(genPersonalSpawn/genInstanceSpawn)
- 【现状】注入 `SETONTIMER {id}` 单参数;OnTimer 段内 `#ACT @元歌个人刷怪-<地图>` 直接跳转,但该段在 QuestDiary 子文件、不在 QManage 内 → 引擎找不到段;动态刷怪同理跳 `@元歌动态刷新`(在 RobotManage 内)。
- 【原版依据】蜗牛 `jsc-preload-strings.txt:11120/11159`:`[@OnTimer88] #CALL [..\QuestDiary\…\刷怪脚本.txt] @WN个人刷怪` + `SETONTIMER 88 5`(两参:编号 间隔秒)。
- 【改法】
  1. `lib.js:2766` → `'SETONTIMER ' + id + ' 60\r\n'`(间隔 60 秒,或由调用方传 `o.interval`);
  2. OnTimer 段改为先 `#CALL` 加载脚本文件再跳转,需给 `injectQManageTimer` 传目标脚本路径,生成:
     ```
     [@OnTimer N]
     #IF
     #ACT
     #CALL [..\QuestDiary\元歌个人刷怪\<地图>.txt] @元歌个人刷怪-<地图>
     BREAK
     ```
     动态刷怪同理 `#CALL [..\QuestDiary\元歌动态刷怪.txt] @元歌动态刷新`。
- 【验证】注入后打开 QManage.txt:段内是 `#CALL` 且路径存在;`SETONTIMER` 带两个参数。

### P0-5 写回编码:全工具写路径强制 GBK,UTF-8 端被写坏(共性根因,优先级最高)
- 【位置】`lib.js:122-125`(encodeStr)+ 所有写路径:`writeMonFile:136`、`replaceScripts:1076`、`injectScript`、`writePortConfig/replacePorts`、`writeItemDesc/writeUnbindList`、`addMonGen/delMonGen:820/838`、`injectQManageTimer:2775`
- 【现状】`encodeStr` 恒 `iconv.encode(str,'gbk')`;`decodeBuf` 却能 detectEncoding 出 UTF-8,读进 UTF-8、写出 GBK → 整文件乱码。
- 【原版依据】虾米 `工具箱_qt_recovered.py:10400-10406` `_drop_encode_text` 保留原编码。
- 【改法】给 encodeStr 加编码参数并让写路径先探测原文件编码:
  ```js
  function encodeStr(str, enc) { if (iconv) return iconv.encode(str, enc || 'gbk'); return Buffer.from(str,'utf8'); }
  function writeTextFile(f, str) {           // 新增统一写助手
    let enc = 'gbk';
    try { if (fs.existsSync(f)) enc = detectEncoding(fs.readFileSync(f)); } catch (e) {}
    fs.writeFileSync(f, encodeStr(str, enc));
  }
  ```
  把上述写路径的 `fs.writeFileSync(f, encodeStr(x))` 全部换成 `writeTextFile(f, x)`(新增文件才默认 gbk)。
- 【验证】对 UTF-8 BOM 的 MonItems 执行一次添加爆率,重开文件仍 UTF-8 不乱码。

### P0-6 动态刷怪:不删原 MonGen 静态行 → 怪量叠加;MONGENEX 尾列错位
- 【位置】`lib.js:2787-2888`(genDynamicSpawn)、`lib.js:2836-2843`(MONGENEX 行构造)
- 【现状】只写 RobotManage/AutoRunRobot,原 MonGen.txt 不动 → 静态刷怪 + 动态补怪/清怪打架;MONGENEX 行把原行 tail 列(interval/time/trigger)原样拼上。
- 【原版依据】蜗牛 `index-342fb122.js:943-964`:把被动态覆盖的静态行从 MonGen.txt 删掉写回;常量池 `bc-constant-pools.txt:1215` MONGENEX 单独构造 `MonGenEx <地图> <X> <Y> <怪> <range> 0 <nameColor> 255 0`。
- 【改法】
  1. genDynamicSpawn 增加参数 `removeStatic`(默认 true):收集被覆盖的 (地图,怪名) 行,按行号从 rawLines 删除,用 `preserveMonGenWrite` 写回;
  2. MONGENEX 行丢弃原尾列,改为 `spawnCmd + ' ' + map + ' ' + x + ' ' + y + ' ' + mon + ' ' + range + ' 0 255 0'`。
- 【验证】生成后 MonGen.txt 中该地图/怪静态行已删;RobotManage 中 MONGENEX 行无多余尾列。

---

## 二、P1 常见路径功能错误

### P1-1 概率为 0/空 → 写入非法 `1/0`
- 【位置】`lib.js:511,513`(addRate)、`main.js:89-97`(do-op 的 child/range 分支)
- 【改法】写入前校验:`const n = Math.max(1, Math.round(+rate||0)); if (!(n>=1)) return {ok:false,…}`;main.js 的 `let n = rate || 0` 后同样钳到 ≥1。原版:虾米 `工具箱_qt_recovered.py:10768` 明确 `rate<=0 → 报错`。
- 【验证】不填概率点添加 → 提示错误,不产生 `1/0` 行。

### P1-2 物品名规范化不完整:删/查/指定追加匹配失败
- 【位置】`lib.js:152-159`(normalizeItemName,缺 `;`/`#` 截断)、`lib.js:508/524`(addRate/delRate 匹配)、`lib.js:673`(findItemDrops)、`lib.js:689`(addAfterItem)
- 【改法】normalizeItemName 追加:`v = v.split(/[;#]/)[0].trim();`;并在 delRate/findItemDrops/addAfterItem 中把 `it.item !== itemName` 改为 `normalizeItemName(it.item) !== normalizeItemName(itemName)`(keys 同样先规范化)。原版:虾米 `工具箱_qt_recovered.py:10690-10698`。
- 【验证】对含 `1/100 书页 RANDOM` 的文件输入"书页"删除 → 命中;物品查询"书页" → 找到。

### P1-3 M2 重载菜单列表与真实菜单错位
- 【位置】`lib.js:4179`(M2_RELOAD_ITEMS 16 项)、`m2-reload.ps1`(菜单枚举)
- 【现状】列表含"开关拿沙/改变城主/删除行会/建立行会/查询地图怪物/复活人物/踢掉人物/杀死人物/小地图配置/管理员列表"等不在"控制-重新加载"菜单的项;真实项(QFunction/QManage/QMission/QChatbox/物品掉落规则/参数设置/地图事件/摆摊/出售/假人等)全缺。
- 【原版依据】虾米 `工具箱_qt_recovered.py:14083-14084` `_get_reload_menu_button_defs` 23 项;翎风实测 22 项(`三合一/m2重载/m2重载.md:60`)。
- 【改法】M2_RELOAD_ITEMS 替换为实测 22 项清单(建议:
  `QFunction、QManage、所有NPC、怪物爆率、Robot、物品、技能、怪物数据库、怪物说话、大血条、宝箱、数据列表、安全区、参数设置、掉落规则、QMission、QChatbox、地图事件、摆摊、出售、假人、授权RunGate`);ps1 中按"控制→重新载入"子菜单枚举的索引顺序与清单一一对应(如 ps1 已按菜单文本匹配则仅改列表)。
- 【验证】GUI 下拉逐项点击,真实 M2Server 上无 NOT_FOUND。

### P1-4 脚本注入变量替换:死循环 + 重叠污染 + 变量类型不全
- 【位置】`lib.js:1442-1446`(新名生成,`+100` 为循环内常量)、`lib.js:1446-1453`(替换未按长度降序)、`lib.js:1410-1422`(VAR_PATTERNS)
- 【改法】
  1. 新名生成改为每次递增:`let cand = m[2].match(/^\d+$/) ? (base+100+n) : (m[2]+'_'+n); n++;` 直到 `!used.has(cand)`(把 `+100` 移入递增表达式);
  2. 替换前对变量列表按名称**长度降序**排序再逐个 `split/join`,避免 `N$签到数` 污染 `N$签到数_1`;
  3. VAR_PATTERNS 补 `J$`/`Z$`、个人标识 `\[(\d+)(?:-\d+)?\]`、U 支持 `U\d{1,3}`(0-499)、T 支持 `T\d{1,3}`;`Q\d{1,2}` 前后加边界避免 `QQ123` 误匹配。
- 【原版依据】蜗牛 `index-ec0ab199.js:111-129`(变量组 U max=499)。
- 【验证】目标文件同时含 `U11`/`U111`、注入内容含 `U11` → 不卡死、不重叠;含 `N$签到数` 与 `N$签到数_1` → 后者不被污染。

### P1-5 开区网站:新区/快餐/单职业三卡片参数被静默丢弃
- 【位置】`lib.js:3768`(读取区,加三键)、`lib.js:3871`(versions 渲染区,加三卡片)
- 【改法】在 3768 后加:
  ```js
  const newZones  = (o.newZones  || '').split(/[,，\n]/).map(s=>s.trim()).filter(Boolean);
  const fastZones = (o.fastZones || '').split(/[,，\n]/).map(s=>s.trim()).filter(Boolean);
  const soloZones = (o.soloZones || '').split(/[,，\n]/).map(s=>s.trim()).filter(Boolean);
  ```
  在 3871 versions 卡片之后追加三块 `.panel`(标题"新区/快餐/单职业",内容 join 换行);renderer.js:503-505 传参键名不变。
- 【验证】GUI 填新区内容 → 生成 index.html 出现对应卡片。

### P1-6 MapInfo 解析:空格/Tab 地图标题被拼进 code
- 【位置】`lib.js:3379-3384`(readMapInfo)
- 【改法】对齐虾米 `工具箱_qt_recovered.py:8246-8254`:`head` 先按 `[\t ]` maxsplit=1 拆出 id,再处理 `|`:
  ```js
  const parts = head.split(/[\t ]+/, 2);          // 先拆 id 与标题
  const code  = parts[0].trim();
  let   title = (parts[1]||'').trim();
  const bar   = code.indexOf('|');
  if (bar >= 0) { title = code.slice(bar+1).trim(); /* code 取 bar 前 */ }
  ```
  与 `exportSiteData:3573-3575` 的拆分保持一致(建议抽公共函数)。
- 【验证】`[3 比奇城]` → code=`3`、title=`比奇城`;两面板显示一致。

### P1-7 兑换/回收 NPC 更新 MerChant 只按 NPC 名匹配,覆盖旧地图行
- 【位置】`lib.js:1204-1210`(addExchangeNpc)、`lib.js:1870`(addRecycleNpc)
- 【改法】匹配条件加地图码:先拆 `t.split(/\s+/)`,`cols[0]===NPC名 && cols[1]===opts.mapCode` 才更新该行,否则追加。原版:蜗牛 `index-eae1c9c2.js:170` 双条件。
- 【验证】同 NPC 换地图二次生成 → 旧地图行保留、新行追加。

### P1-8 存销说明.txt 二次覆盖 + 键名用错
- 【位置】`lib.js:2387`(第一次写)、`lib.js:2406`(try 内二次覆盖)
- 【改法】删除 2406 的第二次 writeFileSync(或改名 `说明-精简.txt`);note 内 `opts.rateType/opts.group/opts.uidType` 改为 `opts.format/opts.groupSize/opts.userIdType`(与表单实参一致)。
- 【验证】生成存销包,说明.txt 含名称/时间/引擎/格式/编号/条数/组数真实值。

### P1-9 货币分析/提取:英文货币名漏映射 + 中文 N$ 变量漏识别
- 【位置】`lib.js:888`(TAKE/GIVE 白名单)、`lib.js:894`(自定义变量正则)
- 【改法】白名单补 `GAMEDIAMOND|GAMEPOINT|GAMEGIRD|CREDITPOINT|GAMEGLORY`;正则字符类 `[A-Z$0-9_.<>]+` → `[A-Z$0-9_.<>\u4e00-\u9fa5]+`。
- 【验证】`TAKE GAMEDIAMOND 100` 记为"金刚石";`DEC N$元宝券 10` 被统计。

### P1-10 端口一键替换误伤非端口字段
- 【位置】`lib.js:1363-1368`(replacePorts)
- 【改法】改为:先用 `readPortConfig(engineRoot)` 拿端口键清单,只对键名是端口键的行替换 `=数字`;或逐行 `if (/^(DBPort|GatePort|LogServerPort|MsgSrvPort|RunGate|SelGate|LoginGate|IDSPort)\d*\s*=/)` 才处理。
- 【验证】含 `IdNum=5000` 的配置文件执行替换 5000→7000,IdNum 不被改动。

### P1-11 原刷怪调整整行重建:8 列变 9 列、对齐丢失
- 【位置】`lib.js:2444-2461`(adjustMonGen 用 serializeSpawn 重建)、`lib.js:783-796`(serializeSpawn)
- 【改法】改为**行级局部更新**:解析出 tokens 后只改目标字段(数量/范围/间隔)的 token,其余列与原始文本串拼接保留;serializeSpawn 不再补 `0` 列(去掉强制 trigger 补列)。原版:虾米 `_render_existing_record` 只替换变化的字段。
- 【验证】8 列行调整数量后仍 8 列、多空格对齐保留。

### P1-12 文件对比:行号对齐误报 + 固定 20 行 + 忽略脚本目录
- 【位置】`lib.js:3471-3486`(compareTextLines)、`main.js:245`(maxLines:20)、`lib.js:3429`(IGNORE_DIRS)
- 【改法】compareTextLines 换 LCS diff(自实现最长公共子序列,单文件环境不加依赖,约 40 行),输出差异块(连续相同段合并);main.js 的 maxLines 提高到 200 或由 GUI 传;IGNORE_DIRS 默认不再忽略 `Market_Def/QuestDiary`(或提供开关)。原版:虾米用 difflib + 左右互拷。
- 【验证】A 插一行,B 后续行相同 → 只报 1 处差异。

---

## 三、P2 边界/兼容/一致性(择要,可分批)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| P2-1 | `lib.js:75-87` monItemsDir | 固定标准 MonItems,MonItemsEx/MonDrop 时落空 | 依次探测 `MonItems`/`MonItemsEx`/`MonDrop`,取存在的第一个 |
| P2-2 | `lib.js:496/431` monsByMap/monstersFromMonGen | 多 token 怪名只取第 4 列 | 复用 readMonGen(754-759)的多 token 收集逻辑 |
| P2-3 | `lib.js:269-275` optimizeRates | 组内去重按物品名,误删同物品不同概率 | key 改 `normalizeItemName(item)+'@'+rate`,与 findDupRates:4138 口径统一 |
| P2-4 | `lib.js:331-334` restoreCall | 同文件重复 #CALL 只展开第一处 | 按行遍历替换(`split(raw).join(target)` 或行级判断) |
| P2-5 | `lib.js:360` addRates | 追加尾部,虾米插头部 | 改为 `items.unshift` 生成的条目 |
| P2-6 | `lib.js:586-596` adjustRateFile type=child | 组外普通行被改写成单元素 #CHILD | 仅对 `kind==='child'` 头行乘倍率,普通 rate 行不动 |
| P2-7 | `lib.js:820/838/834` addMonGen/delMonGen | 硬编码 CRLF;删除留空行 | 用 preserveMonGenWrite(2493)写回;删除行置空改为从 rawLines 移除 |
| P2-8 | `lib.js:2038` salesTimers vs `2053` storeTimerId | 双算法不一致,自身标记块被计占用 | salesTimers 复用 storeTimerId 的"剥离 TOOL_STORE_TIMER 标记块"逻辑 |
| P2-9 | `lib.js:2091-2153` 定时器占满 | 静默生成无定时器脚本 | storeTimerId 返回 0 时 genStoreScript 报错提示 |
| P2-10 | `lib.js:2240-2285` genStoreScript | QF 注入无重名冲突检查 | 注入前扫描 QF 已用 `[@…]` 段名/按钮号,冲突则提示 |
| P2-11 | `lib.js:2363` 新爆率格式 | 输出与物品数据逐字节相同 | 按 `#CHILD 1/N RANDOM` 分组结构单独生成 |
| P2-12 | `lib.js:1895-1906` allocFlags | 普通 `[数字]` 文本误计占用;溢出回退可能重复 | 扫描限定脚本指令上下文;溢出时返回错误而非 res.length |
| P2-13 | `lib.js:3118-3133` readItemDesc/writeItemDesc | 全量重写丢注释、GBK 写回 | 按原行号替换、保留注释/空行,写回用 writeTextFile |
| P2-14 | `lib.js:3101-3103` itemDescFile | 缺 DZItemDescList.txt(备注-上) | 探测 DZItemDescList.txt 作为 top 文件候选 |
| P2-15 | `lib.js:3363-3367` delStartPoint | 同坐标不同类型安全区被一并删除 | 匹配条件加 type(通过 UI 传入所选行类型) |
| P2-16 | `lib.js:3516-3525` healthReport | 缺失目录键静默漏检 | 缺失键 push `{ok:false}` issue,与 scanSetupDirKeys 一致 |
| P2-17 | `lib.js:4053-4058` fixSetupDirKeys | 完全缺失的键不修 | value 为空时按 SETUP_DIR_MAP 建议值补写行 |
| P2-18 | `lib.js:3728-3729` remoteListen | 端口占用 error 被吞,仍报成功 | `server.on('error', e => reject/返回失败)` |
| P2-19 | `lib.js:1461-1471/1494-1496` injectTargetFile | QuestDiary 目标无效;目录路径 EISDIR 崩溃 | 支持 QuestDiary 目标;readFileSync 包 try/catch |
| P2-20 | `lib.js:1046-1082` replaceScripts | 正则模式替换按字面量执行;空 search 死循环 | 入口 `if(!opts.search) return 报错`;matchMode==='regex' 时用 new RegExp 替换 |
| P2-21 | `lib.js:1252-1253` scanPorts vs `1290-1310` readPortConfig | 端口集合不一致(DBPort_MulThread 漏检) | scanPorts 键匹配改用 `含 Port 且 =数字` 同口径 |
| P2-22 | `lib.js:1677-1680` ftpSync | 大小相同跳过不可靠 | 对父目录 list 后按 name 匹配(而非对文件路径 list) |
| P2-23 | `lib.js:1582` addRobot | 刷怪用 GMEXECUTE @MOB(机器人上下文无 GM 权限) | 改 MONGEN/CHECKMAPMONCOUNT 指令;清怪段加 CHECKMAPMONCOUNT 前置 |
| P2-24 | `lib.js:4213-4219` module.exports | 大量重复导出(allocFlags/listAllFiles x3 等) | 清理重复项 |
| P2-25 | `lib.js:1887-1996` | allocFlags/listAllFiles 各重复定义 3 份 | 保留第一份,删后两份 |
| P2-26 | `preload.js:5/65` | pickDir 重复定义(第 5 行被 65 覆盖) | 删第 5 行 |
| P2-27 | `main.js` 顶部 | GPU 崩溃打不开(见 applog.txt FATAL) | `app.whenReady()` 前加 `app.disableHardwareAcceleration();` |
| P2-28 | `renderer.js:450-462/755-783/806/870/889/905/1168-1173` | 8 个按钮执行后零反馈;x-run root 未判空 | 每处补 `appendLog(r.ok ? '✅ '+r.msg : '❌ '+r.msg)`;x-run 加 `if(!root) return` |
| P2-29 | `renderer.js:298/336/366/374/638/662/713/797` | `r.log.join` 未判空 | 统一 `(r.log||[]).join('\n')` |
| P2-30 | `renderer.js:5/9/10/1142` | 残留 `[调试]` 日志刷屏 | 删除 |
| P2-31 | `lib.js:1058-1068` replaceScripts | 空搜索"忽略大小写"分支死循环 | 入口拦截空 search(P2-20 一并修) |

---

## 四、修复顺序建议

1. **P0-1 → P0-2 → P0-3**:货币映射、分组、网站概率,一行级/局部级修改,立即消除错误数据;
2. **P0-5 写回编码**:先做,避免后续每改一处功能都要回头补编码;
3. **P0-4 → P0-6**:刷怪两大核心;
4. **P1 组**(建议顺序:P1-4 注入卡死 → P1-1 非法爆率 → P1-3 M2 重载 → P1-5 网站三卡片 → 其余);
5. **P2 组**按表顺序,每批 5-10 条,改完跑 `node --check lib.js` + 对应验证。

## 五、注意

- 改前 `copy D:\元歌工具箱\GUI\resources\app\lib.js lib.js.bak-日期`;
- 每批改完跑 `node --check D:\元歌工具箱\GUI\resources\app\lib.js`(及 renderer.js/main.js/preload.js);
- CLI 版 `元歌工具箱-CLI.exe` 是独立打包,修复后需用 `snail-reversing` 工作区的 gen-bundle 流程重新打包(见该工作区 `gen-bundle.js`/`fix-gen-bundle-memcode.js` 说明);
- P0-3 与 P2-11 同源(den 语义),建议一起改。

---

## 六、第三轮补充检查(2026-08-10,功能逻辑 + 工具箱 Bug 全面复查)

> 本轮新发现(编号 R3-x)。已并入修复顺序:与既有项冲突的以本节为准。

### 6.1 🔴 重大新发现(必改)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| R3-1 | `renderer.js:1200/1216/1254/1256/1262/1268/1272/1281/1288` | **`qkLog` 函数从未定义**,快捷操作面板 9 处调用全部 ReferenceError:点"刷新 NPC 列表/过滤输入/打开快捷文件/我的快捷/NPC 打开/添加自定义路径"全部运行时崩溃,表格也不渲染 | 补一行 `const qkLog = (msg, cls) => appendLog(msg, cls);`(放在 appendLog 定义之后) |
| R3-2 | `store-template.txt:6/334/357/372/969/1122/1123` + `lib.js:2149/2181` | **存销模板硬编码 `..\QuestDiary\ggjfl\`,而 applyRepl 替换目标是 `QuestDiary\咕咕鸡物品分类`**——模板里根本没有这个串,`categoryFolder` 参数整体失效;生成的主脚本读取 `..\QuestDiary\ggjfl\武器分类.txt` 等不存在路径,分类列表全空白,LineCounts/SearchCache 目录静默写失败,**存销主功能部署即不可用** | 把 applyRepl 替换目标改为模板真实串 `..\QuestDiary\ggjfl\`(替换成 `..\QuestDiary\<categoryFolder>\`),或把 categoryFolder 默认值统一为 `ggjfl`,并保持 QManage 登录块/界面配置读取一致 |
| R3-3 | `lib.js:4147`(checkServerConfig) | **DB 端口一致性正则字面量又一处 `\\s` 笔误**(`/^ServerPort\\s*=\\s*(\d+)/m`),`dbsrc.ini` 永远匹配不上 → 引擎健检把"DB 端口一致性"误报 OK,与 healthReport(3533/3537)同根因但独立函数 | `\\s`→`\s`、`\\d`→`\d`(此函数内两处) |
| R3-4 | `lib.js:188-197`(applyOp 'set') | **set 0 语义反转**:`opValue=0` → res=0 → 钳到 1 → 生成 1/1 必爆。"清零爆率"变"必爆",隐性数据损坏 | set 时若 `opValue<=0` 返回 `MAX_RATE`(90000000,不爆)或明确报错,不要钳成 1 |
| R3-5 | `lib.js:1483`(replaceOccupyVars) | **U 数字前缀覆盖**:注入内容同时含 `U1`/`U10`/`U100` 且都冲突时,`split('U1')` 把 `U10` 改成 `U1010`、`U100` 只提取到 `U10`(`extractVars` 的 `/U\d{1,2}/`),变量错乱且注入失效 | 替换前按名称长度降序;替换用带边界的正则 `new RegExp(v + '(?!\\d)','g')`;`extractVars` 的 U 改成 `U\d{1,3}` |
| R3-6 | `lib.js:2676-2734`(genPersonalSpawn/genInstanceSpawn) | **负数穿透**:`count = parseInt(cfg.count,10) || 1`、`range = parseInt(cfg.range,10) || 3`,输入 `-5` 时生成 `MONGEN 怪 -5 3` 废脚本无提示 | 改 `Math.max(1, parseInt(...)||1)`(count/range 同);`spawnTable` 的 `limit`(2681)同样 `Math.max(0,...)` |
| R3-7 | `lib.js:2708-2723`(genPersonalSpawn) | **同地图多怪重复段头**:按"MONGEN 怪名"判断已存在,但整段(含 `[@元歌个人刷怪-地图]` 段头)整体追加 → 同地图第二种怪生成后文件有多个同 label 段,引擎只取首段,新怪永不刷 | 检测已有段,只把 MONGEN 行追加进现有段内,不重复段头 |
| R3-8 | `lib.js:2986-2998`(quickBackup) | **copyTree 无 try/catch + symlink 无限递归**:权限拒绝/文件占用直接中断整个备份;Envir 内含指向祖先的符号链接会死循环;且 Envir 已含 MonItems 子目录,MonItems 又被单独复制一遍 | 每项操作 try/catch 计入 skipped;copyTree 加 visited Set + 深度上限;复制 Envir 时跳过 MonItems 子目录 |
| R3-9 | `lib.js:3061/3075`(readScriptFile/saveScriptFile) | **路径穿越**:`rel` 未校验,含 `..` 可越出 Envir 读/写任意文件 | `path.resolve` 后校验前缀在 `Envir` 内,否则拒绝 |
| R3-10 | `lib.js:1694/1669/3721`(ftpSync/remotePush) | **外部依赖无兜底**:`require('basic-ftp')` 缺失时同步抛;`fetch` 在 Node<18/SEA 无全局 fetch 抛 ReferenceError;连接失败分支不 `client.close()` | require 包 try/catch 返回 `{ok:false}`;fetch 包 try/catch 返回 `{ok:false,msg:'推送失败: '+e.message}`;所有提前 return 前 close |

### 6.2 🟠 中(常见路径功能错误)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| R3-11 | `lib.js:463-510`(overallOptimize) | aborted 分支已写盘文件不回滚(半应用);monsters 未去重(同一怪处理多次);rangeOp 缺失时静默不过滤 | 先全量计算校验(两遍式)再统一写盘;`Set` 去重;rangeOp 缺失时提示而非静默 |
| R3-12 | `lib.js:2550-2653`(scanVariables) | U/Q 双计(U 既在 VAR_PATTERNS 计数又在单字母表计数;Q 只计数不 push,Q 变量查询结果缺失);byType 声明未用 | VAR_PATTERNS 移除 U/Q 交给单字母表;单字母表补 Q;删 byType |
| R3-13 | `lib.js:3068/3079`(readScriptFile/saveScriptFile) | 返回 size 单位不一致(字节 vs UTF-16 码元),同文件读写后 size 不同,前端"已保存"误判 | 统一 `Buffer.byteLength(content,'utf8')` |
| R3-14 | `lib.js:3117`(readMerChant) | **只按 `/\t+/` 分隔,空格分隔的 MerChant 版本所有 NPC 被 `parts.length<5` 丢弃** | 改 `split(/\s+/)` |
| R3-15 | `lib.js:2196`(genStoreScript) | "创建文件.txt"用 `<$USERID>_<$USERNAME>` 字面名,与全程 storeU 编号路径不一致,新玩家首登创建垃圾 ini 且覆盖编号路径 | 改为 `<$STR(storeU)>.ini` 拼接 |
| R3-16 | `lib.js:2154` + `store-template.txt:1377`(genStoreScript) | `interval` 参数赋值后从未使用,模板硬编码 `SetOnTimer 51 2`,UI 设间隔无效 | applyRepl 一并替换间隔数字,或删参数同步 UI |
| R3-17 | `lib.js:1505/1605`(removeInjectedBlock/delRobot) | name 未转义直接拼正则,注入名含 `(`/`*` 等元字符时 overwrite 删不掉旧块、每次堆叠 | `name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')` |
| R3-18 | `lib.js:840`(addMonGen) | `!s.x || !s.y` 拒绝坐标 0,地图边缘 (0,0) 无法刷怪 | 改 `s.x == null || s.y == null` |
| R3-19 | `main.js:337-342` + `renderer.js:890` | mg-adjust handler 丢弃 `skipped` 字段,排除统计永不显示(skipMsg 死变量) | handler 返回 `{ log, skipped: r.skipped }` |
| R3-20 | `main.js:350` + `lib.js:2932` | dynamic-spawn handler 读 `r.qm`,lib 从不返回该字段,QManage 注入反馈恒丢失 | 改读 `r.timerId`,或 lib 返回 `qm` |
| R3-21 | `m2-reload.ps1:218-228` | `-Item` 为空时 `Contains('')` 恒真,误点击第一个叶子菜单触发任意重载 | 执行前 `if([string]::IsNullOrWhiteSpace($Item)){RESULT:NO_ITEM;exit}` |
| R3-22 | `m2-reload.ps1:141-149` | 窗口选择:不可见 Main/Form 窗口先枚举到会锁死选中;多开无 PID/实例筛选,可能重载到错误的服 | 可见性作为强条件;按 -Root/PID 唯一化 |
| R3-23 | `lib.js:3662` + `droprate.html:1374-1420/2113-2125` | exportSiteData 的 npcs[].scripts/give/take/teleports 恒空数组,网站 NPC 明细(传送走法/交易明细)空白 | 导出时解析 NPC 脚本填充,或前端隐藏空区域 |
| R3-24 | `renderer.js:24` + `index.html` | `out()` 的 5 个目标 id(m-out/c-out/s-out/x-out/rc-out)在 index.html 全部不存在,面板级输出区从未存在 | 补 5 个输出区,或 out() 改无参并删各处 id |

### 6.3 🟢 低(边界/清理)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| R3-25 | `lib.js:1421-1426`(checkPorts) | 逐端口串行 TCP 探测,最坏 n×600ms | 并发限制(如 8 并发) |
| R3-26 | `lib.js:1406-1416`(checkPortInUse) | 仅 TCP 探测,UDP 网关端口误报空闲 | 结果标注"TCP 探测"或加 UDP 探测 |
| R3-27 | `lib.js:644`(adjustRateFile) | 倍率 1 且无任何行变化仍整体写文件,更新 mtime | `if (changed)` 才写 |
| R3-28 | `lib.js:716`(addAfterItem) | 同文件多条目标物品只插第一条,与报告命中数不一致 | 逐行插入所有命中处 |
| R3-29 | `lib.js:1560`(readRobots) | `manageContent` 存的是 AutoRunRobot 内容而非 RobotManage | 存 `mc` |
| R3-30 | `lib.js:576`(adjustItem) | 死代码,导出但从未调用,与 adjustRateFile 并存易分叉 | 删除或统一 |
| R3-31 | `main.js:323-325/111` | npc-list/npc-read/npc-save/quick-backup 四个死 handler(preload 暴露但 renderer 无调用) | 删除或补 UI 入口 |
| R3-32 | `lib.js:3752`(remoteListen) | body 超 64MB destroy 后 res 永久挂起,推送方 fetch 挂到超时 | destroy 前 `res.end(413)` |
| R3-33 | `lib.js:2431-2433`(genSalesData) | 说明文本键名 `opts.rateType/group/uidType` 与同函数实际 `format/groupSize/userIdType` 不一致,显示默认值 | 统一键名 |
| R3-34 | `lib.js:2694-2701`(detectDungeonMaps) | `[A|B|C 名称]` 第三代码被忽略 | 循环处理所有副代码 |
| R3-35 | `main.js:276`(preview-site) | close 旧 server 后不置 null(当前无竞态,卫生问题) | 顺手置 null |
| R3-36 | `lib.js:3040`(listNpcFiles) | stat 无 try/catch,文件被并发删除时列表中断 | stat 包 try/catch continue |

### 6.4 本轮已核验无误(排除项,供参考)

- 存销定时器分配主体、genUserId 三格式、兑换档位 ×10/×100、回收 TakeBagItem/货币给予指令(前轮已确认);
- droprate.html 消费字段与 exportSiteData 输出(`item_drops/map_index/routes/spawns`)一致;
- m2-reload.ps1 的菜单枚举/WM_INITMENU 预热/MF_BYPOSITION/递归叶子逻辑;
- index.html 与 renderer 的 `$('id')` 除 out() 5 个目标外双向闭合;
- health-check/compare-dirs/spawn-table/store 系列/port-config/item-cfg/map-cfg/engine-check 等 handler 返回与 renderer 消费字段匹配。

### 6.5 第三轮最值得先修的 5 个(按性价比)

1. **R3-1 `qkLog` 未定义** — 补 1 行定义,快捷操作面板整体复活(UI 最高频崩溃);
2. **R3-2 存销 ggjfl/categoryFolder 断链** — 改 1 处替换目标字符串,存销主功能恢复可用;
3. **R3-3 checkServerConfig `\\s` 笔误** — 2 个字符,引擎健检 DB 端口检查恢复;
4. **R3-4 applyOp set 0 语义反转** — 1 处判断,"清零爆率"不再变"必爆";
5. **R3-5 U 数字前缀污染** — 边界正则 + 降序替换,注入变量不再错乱。

---

## 七、修复验收记录(2026-08-11)

> 验收对象:`D:\元歌工具箱\GUI\resources\app\` 下 lib.js / main.js / renderer.js / preload.js / m2-reload.ps1(改于 8/11 02:06-02:26)。
> 方法:逐项对照本清单静态核验 + `node --check` 四个 JS 文件全部通过。
> 结果:**17 项已修 ✅ / 24 项未修 ❌**。以下行号为验收时的新行号。

### 7.1 ✅ 已修(17 项,无需再动)

| 编号 | 修复内容 | 验证位置 |
|---|---|---|
| P0-1 | 货币映射:gameglory→'荣誉'、gamegird→'灵符' | lib.js:1134/1137 |
| P0-2 | groupRates 组头保留分母 + 括号深度跳过块内 | lib.js:247/257-265 |
| P0-3 | 网站 den 换算 `num>1?round(rate/num):rate` | lib.js:3677/3681 |
| P0-4 | SETONTIMER 两参 + OnTimer 段 #CALL 加载 | lib.js:2782/2799/2807 + 2727/2761 |
| P0-5 | writeTextFile 保留原编码与 UTF-8 BOM | lib.js:122-141(写路径已切换) |
| `\\s`×5 | healthReport + checkServerConfig 正则笔误 | lib.js:4149 等 |
| GPU | app.disableHardwareAcceleration() | main.js:602 |
| qkLog | 补定义 | renderer.js:35 |
| 空搜索 | replaceScripts 入口拦截 | lib.js:1077 |
| 注入死循环 | 新名循环内递增 +100+n | lib.js:1476-1479 |
| addRates | 概率钳制 + 物品名规范化 | lib.js:371/373 |
| 路径穿越(injectTargetFile) | Envir 前缀校验 | lib.js:1496-1501 |
| 坐标 0 | addMonGen `== null` | lib.js:840 |
| 存销 ggjfl | 替换目标对齐模板真实串 | lib.js:2183 |
| ps1 Item 空 | IsNullOrWhiteSpace → NO_ITEM | m2-reload.ps1:218 |
| 按钮反馈 | 12+ 处补 appendLog(✅/❌) | renderer.js:539-857 |
| 语法 | node --check 4 文件全过 | — |

### 7.2 ❌ 未修(24 项)——之前窗口续改清单

**功能类(12 项):**

| 编号 | 问题 | 当前行号 | 缺什么 / 怎么改 |
|---|---|---|---|
| F-P1-5 | 网站"新区/快餐/单职业"三卡片参数被丢弃 | lib.js:3768/3871 | 读取 `newZones/fastZones/soloZones` 并在 versions 卡片后渲染三块 .panel |
| F-P1-3 | M2 重载菜单仍旧 16 项(错项未删、真实项未补) | lib.js:4230 | 替换为 22 项实测清单(见 6.2 或功能bug.md F-P1-3) |
| F-P2-11 | 存销"新爆率格式"仍输出与物品数据逐字节相同的 JSON | lib.js:2393-2395 | 按 `#CHILD 1/N RANDOM` 分组结构单独生成(与 P0-3 den 语义一起) |
| F-P1-8 | 存销 说明.txt 仍写两次互相覆盖、键名错 | lib.js:2419/2438 | 删第二次写入;note 用 `format/groupSize/userIdType` |
| F-P1-7 | 兑换/回收 NPC 更新 MerChant 无地图码,换地图覆盖旧行 | lib.js:1235(回收 1870 同) | `cols[0]===NPC名 && cols[1]===mapCode` 双条件 |
| F-P2-10 | genStoreScript 注入 QF 无段名/按钮号冲突检查 | lib.js:2263-2320 | 注入前扫描 QF 已用 `[@…]` 段与 `@ButtonClickN` |
| R3-5 | 注入 U 数字前缀污染(U1 改 U10 成 U1010) | lib.js:1485 | 替换前按长度降序;`new RegExp(v+'(?!\\d)','g')` 边界替换 |
| R3-7 | 个人/副本刷怪同地图多怪重复段头,新怪不刷 | lib.js:2724/2758 | 检测已有 `[label]` 段,只追加 MONGEN 行进段内 |
| R3-14 | readMerChant 只按 Tab 分隔,空格分隔版 NPC 全丢 | lib.js:3119 | `split(/\t+/)` → `split(/\s+/)` |
| F-P1-6 | readMapInfo `[3 比奇城]` 地图标题拼进 code | lib.js:3433 | 先 `split(/[\t ]+/,2)` 拆 id 再切 `\|`(与 exportSiteData 统一) |
| R3-12 | scanVariables 单字母表无 Q;U 双计 | lib.js:2562/2609 | GROUPS 常规类型补 'Q';核对 U/T 只计一次 |
| R3-16 | genStoreScript interval 仍无效(模板硬编码间隔 2) | lib.js:2156/2185 | 2185 替换 51→timerId 后,把 `\d+` 间隔一并替换为 interval |

**工具箱类(12 项):**

| 编号 | 问题 | 当前行号 | 缺什么 / 怎么改 |
|---|---|---|---|
| R3-6 | 刷怪 count/range 负数穿透(`-5||1=-5`) | lib.js:2712/2735/2681 | `Math.max(1, parseInt(...)||1)`;limit 用 Math.max(0,…) |
| R3-9 | readScriptFile/saveScriptFile 路径穿越 | lib.js:3063/3077 | path.resolve 后校验 Envir 前缀(照抄 injectTargetFile 1496-1501 的写法) |
| R3-13 | read/saveScriptFile size 单位不一致 | lib.js:3070/3081 | 统一 `Buffer.byteLength(content,'utf8')` |
| 端口预设 | applyPortPreset 正则仍 `'\s*=\s*'`(单反斜杠被吞) | lib.js:3363 | 改 `'\\s*=\\s*'`、`'\\d+'`(与 writePortConfig 1345 对照) |
| CRLF+空行 | addMonGen/delMonGen 硬编码 CRLF、删除留空行 | lib.js:850/864/868 | 用 preserveMonGenWrite;删除从 rawLines 移除整行 |
| 单怪 1/0 | main.js:89 `rate||0` + lib.addRate 内部 `rate: rate||0` 都没钳制 | main.js:89 / lib.js:530-540 | 两处都加 `Math.max(1, Math.round(+x||1))`(addRates 已修,单怪漏了) |
| remoteListen | error 被吞假成功;超 64MB 无 413 | lib.js:3754/3780 | error 记录失败;handler 返回前查 `server.listening`;destroy 前 `res.end(413)` |
| R3-15(半修) | 创建文件.txt:2181 模板替换已改,但 2198 **手工拼接段仍是字面 `<$USERID>_<$USERNAME>`** | lib.js:2198 | `mov S$用户数据存储地址 …+zoneSlash+'<$STR(storeU)>.ini'` |
| i-run | 脚本注入执行后仍无输出(其余按钮都补了,唯独漏这条) | renderer.js:461 | `const r = await …doInject(…); appendLog(r.log ? r.log.join('\n') : (r.msg||''), r.ok?'ok':'err');` |
| r.log.join | 13 处仍裸 join(只修了 915 一处) | renderer.js:299/319/325/337/367/375/639/663/714/737/738/741/742/798 | 统一 `(r.log||[]).join('\n')` |
| 调试日志/死handler/pickDir | `[调试]` 4 处、quick-backup/npc-list/read/save 死 handler、preload pickDir 重复 | renderer.js:5/9/10/1143;main.js:111/323-325;preload.js:5/65 | 删调试行;删死 handler 或补 UI;删 preload 第 5 行 |
| module.exports | 重复导出原样(allocFlags/listAllFiles ×3、addExchangeNpc ×2 等) | lib.js:4268 | 清理重复键 |

### 7.3 续改建议

1. 优先 **单怪 1/0**(main.js:89 + lib.js:530-540,两行)与 **i-run 无反馈**(renderer.js:461,一行)——用户高频操作;
2. 其次 **R3-15 半修**(lib.js:2198,一行)与 **r.log.join 13 处**——改完即可避免崩溃;
3. 功能类按 7.2 表逐项,需对照原版素材的:M2 菜单 22 项(虾米 `工具箱_qt_recovered.py:14083`)、readMapInfo(虾米 `8246-8254`)、QF 冲突检查、新爆率格式;
4. 每批改完 `node --check lib.js / renderer.js / main.js`;lib.js 改前先备份。

---

## 八、第二轮验收记录(2026-08-11 04:47,24 项续改后)

> 验收对象:8/11 03:29-03:44 修改的 lib.js(227133B)/ main.js / renderer.js / preload.js。
> 方法:24 项逐项静态核验 + `node --check` 四文件全过。
> 结果:**22 项完全修复 ✅ / 2 项未完全 ⚠️**。3 项独立修正合理。

### 8.1 ✅ 完全修复(22 项)

| 编号 | 验证要点 | 位置 |
|---|---|---|
| 1.1 | addRate 单怪 rate/count 钳制 `Math.max(1,…)` | lib.js:538/540 |
| 1.2 | i-run 反馈 appendLog | renderer.js:460 |
| 1.3 | 创建文件.txt `<$STR(storeU)>.ini`,无 USERID 字面残留 | lib.js:2213 |
| 2.1 | count/range 负数钳制 | lib.js:2753/2754/2787/2788 |
| 2.2 | read/saveScriptFile Envir 前缀校验 | lib.js:3126-3128/3144-3146 |
| 2.3 | size 统一 Buffer.byteLength utf8 | lib.js:3136/3151 |
| 2.4 | applyPortPreset `\\s*=\\s*` 双反斜杠 + escape 补 `[\]\\` | lib.js:3433 |
| 2.5 | addMonGen/delMonGen preserveMonGenWrite + 整行移除 + 末尾空串清理 | lib.js:850-853/866/870-871 |
| 2.7 | 调试日志删净;死 handler 删净;pickDir 唯一且 main.js:266 有 `pick-dir` handler(未引入新 bug) | renderer/preload/main |
| 2.8 | module.exports 抽查 10 项均去重 | lib.js:4348+ |
| 3.1 | M2_RELOAD_ITEMS 22 项实测清单,无"开关拿沙" | lib.js:4314 |
| 3.2 | readMapInfo 先 `\|` 再空白拆 code,title 回退 `\|` 后 | lib.js:3503-3506 |
| 3.3 | QF 注入前扫描 `[@qrMethod]`/`[@ButtonClickN]` 冲突提示 | lib.js:2283-2287 |
| 3.4 | 新爆率格式按 #CHILD 分组结构(组头保分母/组内 1/1/depth 透传,与 groupRates 同语义) | lib.js:2414-2447 |
| 3.5 | newZones/fastZones/soloZones 读取+三卡片渲染 | lib.js:3895/4002-4003 |
| 3.6 | 说明.txt 单次写入、真实键名 | lib.js:2471-2480 |
| 3.7 | 兑换/回收 MerChant NPC 名+地图码双条件 | lib.js:1238-1246/1909-1917 |
| 3.8 | U 前缀:长度降序 + `(?!\d)` 边界 + 函数替换 + escape | lib.js:1474/1492 |
| 3.9 | 段头重复:已有段内 BREAK 前插 MONGEN 行 | lib.js:2765-2776 |
| 3.10 | readMerChant `split(/\s+/)` | lib.js:3189 |
| 3.11 | scanVariables 常规类型补 'Q' | lib.js:2603 |
| 3.12 | SETONTIMER 51→timerId 后间隔一并替换为 interval | lib.js:2196-2200 |

### 8.2 ⚠️ 未完全(2 项,建议补)

| 编号 | 问题 | 位置 | 补法 |
|---|---|---|---|
| 2.6 | remoteListen error 已记录到 `server._listenErr`(lib.js:3854),但 **main.js handler 未检查** → 端口占用时仍返回 `ok:true` 假成功 | main.js:224-230 | handler 返回前检查:`setTimeout(()=>{ if (r.server._listenErr || !r.server._ready) return 失败 }, 50)` 或在 handler 内同步查 |
| 1.4 | renderer 736-741 仍 4 处裸 `s.log.join`/`f.log.join`(经确认 handler 恒返回 log 字段、不崩,仅样式未统一) | renderer.js:736/737/740/741 | 顺手改 `(s.log||[]).join` 统一风格 |

### 8.3 独立修正核验

- applyRepl 占位符隔离方案(防 scriptName 含"咕咕鸡过滤"二次替换):✅ 已确认 lib.js:2170-2177 占位符 P 方案;
- readMapInfo `[4\|盟重省]` title 回退:✅ 已确认 3503-3506;
- 未改 P0-6(0 255 0)与货币方向判断:✅ 理由成立(原版清除段确有 0 255 0;GameGird=灵符 方向正确)。

### 8.4 结论

质量高:U 前缀、段头插入、新爆率格式、QF 冲突检查等几处改法专业且与原版语义一致;四文件语法全过。剩余 2 项是小尾巴,补完即可收尾。

### 8.5 第三次核验(2026-08-11 04:56,2 项小尾巴补完后)

- 2.6 remote-listen:✅ 已补——main.js:230-236 handler 等 120ms 后检查 `_listenErr || !_ready`,端口占用时 close + 返回 `{ok:false,msg:'端口被占用…}`;
- 1.4 renderer 4 处:✅ 已补——736/737/740/741 统一 `(s.log||[])`/`(f.log||[])`;
- `node --check` 四文件全过。
- **结论:24/24 全部修复完成,无遗留。**

---

## 九、实测记录(2026-08-11,最新版)

> 测试方式:① 实际启动 `GUI\元歌工具箱.exe`;② mock iconv + 构造临时传奇端目录,直接加载 lib.js 跑 30+ 功能断言。
> 结果:**GUI 启动正常(软件 bug 无);功能层发现 3 个真实未修 bug**(其中 2 个是清单漏项/漏列,非窗口漏改;1 个需复核)。den 换算、addRate 钳制、MonGen 7 列增删、双地图 NPC、存销 4 脚本、体检端口、注入边界、三卡片、说明.txt 等 25+ 项断言全部通过。

### 9.1 GUI 启动验证 ✅

- 启动后 4 个进程存活、主窗口标题"元歌工具箱"、Responding=True;
- applog.txt 无新增错误(0806 的 GPU FATAL 为历史日志)——`disableHardwareAcceleration` 修复生效。

### 9.2 🔴 真实未修功能 bug(3 个,需之前窗口补)

| # | 位置 | 问题 | 复现 | 改法 |
|---|---|---|---|---|
| **G1** | `lib.js:787`(readMonGen) | **MonGen 6 列行(无间隔列)仍被丢弃**:`if (i2 + 2 >= p.length) continue;` 应为 `>` | 测试端写入 `3 335 335 钉耙猫 30 12`,解析结果缺"钉耙猫"(只剩多钩猫/半兽人);连锁:该行在刷怪表格/按地图刷怪/动态刷怪/网站刷新数据中漏掉,`delMonGen` 也删不掉它 | `>=` 改 `>`(并核对 6 列时 range=p[i2]、count=p[i2+1] 取值正确) |
| **G2** | `lib.js:921`(matchCurrencyCmd) | **TAKE/GIVE 白名单缺英文货币**:只有 `金币\|元宝\|金刚石\|灵符\|游戏点\|声望\|荣誉\|GOLD\|GAMEGOLD`,缺 `GAMEDIAMOND/GAMEPOINT/GAMEGIRD/CREDITPOINT/GAMEGLORY`;`TAKE GAMEDIAMOND 50` 被记为自定义货币"GAMEDIAMOND",统计拆散;DEC/INC 正则字符类无中文,`DEC N$元宝券 10` 整行漏计 | 实测 currencyReport 货币列表 = [金币, GAMEDIAMOND, 元宝](无"金刚石"、无"N$元宝券") | 921 行白名单补 5 个英文 key + names 映射;DEC/INC 正则 `[A-Z$0-9_.<>]+` → 加 `\u4e00-\u9fa5` |
| **G3** | `lib.js:3251-3254`(writeItemDesc) | **物品备注写回丢注释**:writeItemDesc 用 `items.map(...)` 全量重写,readItemDesc 读时跳过的 `;` 注释/表头全部丢失 | 构造 ItemDescList.txt 含 `;注释` 行,执行 addItemDesc 后注释消失 | 按原行号替换(保留注释/空行/顺序),写回沿用 writeTextFile |

> 说明:G2/G3 在总清单 P1-9 / P2-13 有记录,但第二轮"7.2 未修 24 项"漏列(验收时未逐一覆盖);G1(6 列行)是总清单漏项——三处均已补入本表。

### 9.3 ✅ 实测通过的关键功能(25+ 断言)

- 爆率:parseLine 全格式(普通/比例/CHILD/括号)、addRate rate=0 钳 1、delRate 命中 RANDOM 后缀与 //注释行、groupRates 组头保分母不嵌套;
- 刷怪:7 列行增删、MONGENEX 解析、坐标 0 合法、LF 换行保留(未转 CRLF);
- 货币:金币/元宝识别;兑换 NPC 同 NPC 两地图两行(双条件);脚本 GameGold 指令正确;
- 网站:den 换算 1/100→100、1/2→2、5/10→2;map_index `[3 比奇城]`→code=3;三卡片渲染;
- 存销:说明.txt 单次+真实键名、新爆率格式输出 #CHILD 结构、genStoreScript 4 脚本+interval 替换;
- 体检:DB 端口不一致正确报 issue;
- 注入:U1 冲突不污染 U10(边界+降序);
- 全部 `node --check` 通过。

### 9.4 测试方法备注(可复现)

- mock `global.__iconvLite`(decode 支持 gb18030/utf8,encode 恒 utf8)后 `require lib.js`;
- 临时目录构造 `Mir200/Envir/MonItems|MonGen.txt|MerChant.txt|MapInfo.txt|StartPoint.txt|QFunction-0.txt|QManage.txt` + `!setup.txt` + `DBServer/dbsrc.ini` + `Config.ini`;
- 断言脚本已按"干净目录隔离"原则重跑,排除顺序副作用。

### 9.5 给之前窗口的补改清单(3 项)

1. G1:`lib.js:787` `i2 + 2 >= p.length` → `>`(MonGen 6 列行不再丢);
2. G2:`lib.js:921` TAKE/GIVE 白名单补 5 个英文货币 + DEC/INC 正则加中文字符类;
3. G3:`lib.js:3251` writeItemDesc 改为按原行号替换、保留注释。
补完跑 `node --check lib.js`,并可用 `Mir200/Envir/MonGen.txt` 加一行 6 列刷怪行验证 G1。

---

## 十、第四轮全面复测(2026-08-11 20:53,补完 G1-G3 后)

> 方法:G1/G2/G3 逐项代码核验 + A/B/C 三域 44 项断言(干净目录隔离,mock iconv)+ GUI 实际启动。
> **结论:全部通过,未发现新的软件/功能 bug。G1-G3 修复有效。**

### 10.1 G1-G3 修复核验 ✅

| 项 | 修复位置 | 复测结果 |
|---|---|---|
| G1 6 列行 | lib.js:787 `i2 + 2 > p.length` | 6 列行解析/range/count/删除全过 |
| G2 货币 | lib.js:921 白名单补 5 英文 + 927 中文 `\u4e00-\u9fa5` | GAMEDIAMOND→金刚石、N$元宝券/N$在线时长 识别全过 |
| G3 物品写回 | lib.js:3499 按原行号替换 + 3528 删除留行 | 注释/空行保留、追加/删除正确 |

### 10.2 A/B/C 三域 44 项断言结果 ✅

- A 域(爆率+刷怪)8/8:6 列解析、addRate 钳制、delRate 注释行、delRates、groupRates 组头/不嵌套/组内 1/1;
- B 域(货币+兑换+回收+物品)12/12:G2 四项、extractCurrencies、双地图 NPC、兑换脚本、回收 NPC、G3 五项;
- C 域(存销+网站+配置+工具)24/24:den 100/2/2、map_index、三卡片、说明.txt、新爆率 #CHILD、genStoreScript(补 zoneFolder 后含 interval)、storeTimerId、genUserId、detectEngine、healthReport、checkServerConfig、scanSetupDirKeys、add/delStartPoint、search/replaceScripts、injectScript/isInjected、quickBackup、scanVariables、checkInputScript、readMerChant 空格、filterNpcs。

### 10.3 GUI 实测 ✅

- 启动:4 进程 + 1 主窗口"元歌工具箱"、Responding=True;applog 无新增错误;
- **注意**:曾观察到 2 个主窗口/8 进程,杀净后重启为 1 窗口/4 进程——**是上轮残留实例,非多开 bug**(元歌无多开检测,重复启动会并存,属已知行为;如需可加单实例锁)。

### 10.4 本轮 21KB 改动的已知新特性(非 bug)

- genStoreScript 新增必填 `zoneFolder`(区服名称),renderer.js:664-670 自动探测填入、index.html 有 `ss-zone` 输入框——GUI 与 lib 一致;
- 其余 FAIL 均为测试环境缺文件(无 M2Server.exe / 未建 ChatDir 目录),非产品问题。

### 10.5 结论

- 软件 bug:无(启动/窗口/语法全正常);
- 功能 bug:无(G1-G3 已修,44 项断言全过);
- 交付可继续;如需彻底收尾,可考虑:① 给 GUI 加单实例锁(避免双开并存);② CLI 版按最新 lib.js 重新打包(前一轮已做 bundle+postject,此轮 lib.js 又有 +21KB 改动,需重打)。
