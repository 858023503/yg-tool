# 元歌工具箱 · 功能测试清单

> 引擎根目录：`D:\cqzs\Mirserver怀念毕业端`
> 测试前建议先点顶部「备份 MonItems」；写操作（会改文件）已标注 ⚠️

---

## 0. 通用准备

1. 打开 `D:\元歌工具箱\GUI\元歌工具箱.exe`
2. 顶部根目录框 → 点「📁 浏览…」→ 选 `D:\cqzs\Mirserver怀念毕业端`
3. 验证：操作日志显示「已选择引擎根目录: D:\cqzs\Mirserver怀念毕业端」
4. 点「备份 MonItems」→ 日志显示备份完成 ✓

---

## 1. ⚡ 快捷操作（左侧第一个）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1.1 | 点「刷新 NPC 列表」 | 日志显示「读取 NPC: 118 个」之类，右侧列表出现 NPC |
| 1.2 | 地图框输入 `3` | NPC 列表只显示地图 3 的 NPC |
| 1.3 | NPC 框输入 `回收` | 列表过滤出名字含"回收"的 NPC |
| 1.4 | 点某 NPC 的「打开」 | 日志显示「已打开 NPC 脚本: xxx」且该脚本文件被打开 |
| 1.5 | 左边「快捷文件」点 `QManage.txt` | 该文件被系统打开（记事本/编辑器）|
| 1.6 | 「我的快捷」输入 `Mir200\Envir\Market_Def` 点添加 | 出现在"我的快捷"列表，点击可打开 |

---

## 2. 📦 爆率调整

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 2.1 | 范围选「指定怪物」，怪物名 `白野猪`，操作类型「追加爆率」，爆率类型「普通爆率 1/N」，爆率 `500` 数量 `1`，物品名 `测试物品`，点「执行修改」⚠️ | 日志「✓ 白野猪: 已追加 1 条」；`白野猪.txt` 末尾多一行 `1/500 测试物品` |
| 2.2 | 「按地图」`shuai209`，追加 `1/100 测试物品2` | 日志列出该地图所有怪各追加 1 条 |
| 2.3 | 「按物品(加在物品后)」目标物品 `书页` 追加 `1/200 测试物品3` | 所有掉书页的怪，书页条目后追加一行 |
| 2.4 | 删除：范围「指定怪物」白野猪，类型「删除爆率」，物品 `测试物品` | 日志「✓ 已删除 1 条」；文件里测试物品消失 |
| 2.5 | 验证删除干净 | 「物品产出查询」搜 `测试物品` → 查无 |

> ⚠️ 2.1~2.4 会改真实端文件，测完记得删除测试物品，或先备份

---

## 3. 📊 批量调整

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 3.1 | 范围「全服调整」，类型「普通爆率 1/*」，倍率 `2`，执行 ⚠️ | 日志「已处理 N 个文件」；`白野猪.txt` 的 `1/100 书页` → `1/200 书页`（分母×2）|
| 3.2 | 倍率 `0.5`，执行 ⚠️ | 分母×0.5（`1/200 → 1/100`，掉率翻倍）|
| 3.3 | 类型「随机爆率 #CHILD 1/*」，倍率 `1`，范围「按怪物」`白野猪` ⚠️ | 组外条目转成：
```
#CHILD 1/100 RANDOM
(
1/1	书页
)
```
（组头格式，物品在组内）|
| 3.4 | 倍率 `2` 再执行 | 随机组头 `1/100 → 1/200`，组内条目不变 |
| 3.5 | minRate `100` maxRate `1000` | 只调整分母在 100~1000 之间的条目 |

---

## 4. 🎲 随机转换

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 4.1 | 范围「按怪物」`白野猪`，minRate 留空，执行 ⚠️ | 组外普通条目（金币2000、书页）→ 随机组格式；原 #CHILD 组保持原样 |
| 4.2 | 打开 `白野猪.txt` 检查 | 格式：
```
#CHILD 1/1 RANDOM
(
1/1	金币2000
)
#CHILD 1/100 RANDOM
(
1/1	书页
)
```
原随机组（战神盔甲那组）原样保留，不嵌套 |
| 4.3 | 「按怪物」选其他怪试一次 | 同样只转组外条目 |
| 4.4 | 测完恢复 | 用备份目录还原（`MonItems\蜗牛备份_QQ群835162076\`）|

---

## 5. 🗺️ 按地图查看（只读）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 5.1 | 地图代码 `shuai209` | 显示该地图刷的怪（青龙、沉默战尊等）+ 每怪爆率文件行数/前 10 条 |
| 5.2 | 地图代码 `3` | 显示练功师等 + 爆率概况 |

---

## 6. 🔍 物品产出查询（只读）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 6.1 | 物品名 `书页` | 显示所有掉落书页的怪（约 293 个）+ 各自爆率行 |
| 6.2 | 物品名 `屠龙` | 显示掉屠龙的怪及爆率 |

---

## 7. 👹 刷怪配置（MonGen）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 7.1 | 「查看」地图 `shuai209` | 显示该地图刷怪行（范围/数量/间隔列正确：`Q004 千年尸王 范围40 数量30`）|
| 7.2 | 「追加」地图 `3` X `300` Y `300` 怪 `测试怪` 数量 `1` 范围 `0` 间隔 `10` ⚠️ | `MonGen.txt` 追加行 `3 300 300 测试怪 0 1 10`（范围在前、数量在后）|
| 7.3 | 「删除」刚追加的测试怪 ⚠️ | 该行移除 |
| 7.4 | 原刷怪调整：范围「全服」，刷新数量 `2` 倍，排除刷新时间留空 | 日志「数量调整 N 行 / 时间调整 0 行」；`Q004 千年尸王 数量30 → 60` |
| 7.5 | 刷新时间 `0.5` 倍 | 间隔 60 分 → 30 分 |
| 7.6 | 动态刷怪：触发百分比 `50`、刷怪百分比 `50` 等，执行 ⚠️ | 生成 AutoRunRobot + RobotManage 段（CHECKHUMCOUNT/MONGEN 数量在前）|

---

## 8. 🤖 机器人脚本（只读为主）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 8.1 | 「读取」 | 显示真实端 23 条定时机器人行（RUNONDAY/RUNONWEEK 全识别）|
| 8.2 | 「添加」一条测试规则 ⚠️ | 追加到 AutoRunRobot.txt，读回可见 |
| 8.3 | 「删除」刚加的 | 移除 |

---

## 9. 💰 货币消耗分析

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 9.1 | 「一键提取全服货币」 | 提取 39 种货币，下拉框自动填充（金币/元宝/金刚石/灵符/游戏点/积分等）|
| 9.2 | 选 `金币` 点「分析」 | 统计全服脚本里金币的 CHECK/TAKE/GIVE 消耗情况 |
| 9.3 | 选 `元宝` 点「分析」 | 元宝消耗统计（CHECKGAMEGOLD/GAMEGOLD）|

---

## 10. 🔄 货币兑换（生成 NPC）⚠️

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 10.1 | 地图代码 `3` X `200` Y `200` NPC名 `元歌货币兑换` | 生成 `Market_Def\元歌货币兑换-3.txt` + MerChant 注册 |
| 10.2 | 用货币 `金币` 数量 `1000`，换 `元宝` 数量 `10`，生成 | 脚本含 `CHECKGOLD 1000`（金币无操作符）+ `TAKE 金币 1000` + `GAMEGOLD + 10`；菜单显示「1000金币兑换10元宝」|
| 10.3 | 用货币 `元宝` 换 `金刚石` | `CHECKGAMEGOLD > 数量`（带操作符）|
| 10.4 | 用货币 `自定义`（货币名 `蜗牛币` 变量 `U11`）| `CHECK U11 数量` / `DEC U11` / `INC U11`（变量逻辑）|
| 10.5 | count=2 | 额外生成 ×10/×100 档位菜单 |

---

## 11. ♻️ 回收 NPC（生成器）⚠️

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 11.1 | 地图代码 `3` X `201` Y `201` NPC名 `元歌回收` | 生成脚本 + MerChant 注册 |
| 11.2 | 加分类：分类名 `武器类` 颜色 `7`，物品 `屠龙|裁决之杖`，金币 `500` | 脚本含 `[@元歌回收]` 勾选界面、`N$元歌回收数量`、总开关、全选/反选 |
| 11.3 | 加第二分类 `衣服类`，物品 `天魔神甲`，元宝 `1` | 两个分类各自独立勾选行 |
| 11.4 | 自定义货币：货币名 `蜗牛币` 变量 `U11` 数量 `5` | `INC U11 5` 逻辑 |
| 11.5 | 生成后打开脚本检查 | 无 `蜗牛`、无 QQ 群、无引流注释；品牌全部是「元歌」|

---

## 12. 📝 脚本搜索/替换

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 12.1 | 搜索 `GAMEGOLD +`（Envir 目录）| 列出所有含该指令的文件+行号 |
| 12.2 | 替换：`旧文本` → `新文本`，范围选一个文件 ⚠️ | 只改目标文件，日志显示替换 N 处 |

---

## 13. 🔌 端口检测（只读）

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 13.1 | 端口范围 `7000`~`7010` | 列出端口状态：■占用 / □空闲 |
| 13.2 | 本机起一个服务占 34567 再测 | 34567 显示占用 |

---

## 14. 📥 脚本注入 ⚠️

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 14.1 | 目标文件选 `QManage.txt`，注入类型「追加」，内容一段测试 | 文件末尾追加，含 `;@@yge-inject:` 标记（无 QQ/蜗牛）|
| 14.2 | 类型「覆盖」| 整文件替换 |
| 14.3 | 类型「取消注入」| 移除之前注入的标记段 |
| 14.4 | 变量占用替换：含 `N$签到数` 的脚本 | 自动替换为未占用变量 `N$签到数_1` |

---

## 15. 📡 目录同步（FTP）⚠️

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 15.1 | 本地起 FTP 服务（可用测试服务器），填主机/端口/用户/密码 | 连接成功 |
| 15.2 | 规则 `Mir200/Envir=/Envir`，点开始同步 | 上传文件，日志显示成功/跳过数（大小对比跳过）|
| 15.3 | 二次同步 | 相同文件跳过（大小一致）|

---

## 16. 🗂️ 存销系统 ⚠️

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 16.1 | 生成路径 `D:\存销测试`，数据目录名 `ceshi` | 生成 5 阶段目录（爆率打包/数据）|
| 16.2 | 爆率格式选「新爆率格式」分组 `100` | 生成 `#CHILD 1/100 RANDOM` 组格式（对齐原版）|
| 16.3 | 用户编号 `数字` / `字母` / `数字+字母` | 编号格式分别 1A / A / 1 三格式正确 |
| 16.4 | 详细逻辑见 `D:\元歌工具箱\存销系统说明.md` | 输出与说明一致 |

---

## 测试结论记录

| 功能 | 结果（✓/✗） | 备注 |
|---|---|---|
| 快捷操作 | | |
| 爆率调整 | | |
| 批量调整 | | |
| 随机转换 | | |
| 按地图查看 | | |
| 物品产出查询 | | |
| 刷怪配置 | | |
| 机器人脚本 | | |
| 货币消耗 | | |
| 货币兑换 | | |
| 回收 NPC | | |
| 脚本搜索 | | |
| 端口检测 | | |
| 脚本注入 | | |
| 目录同步 | | |
| 存销系统 | | |
| 原刷怪调整 | | |
| 动态刷怪 | | |
